drop table if exists network cascade;

create table network
    (
    sec varchar primary key,
	geom geometry,
	buff geometry,
	meas_len float,
	one_way bool,
	road varchar,
	rbt bool,
	benchmark bool
    );